For others directory has `rwx`, specifically `wx`. However, the file inside it there is only `r` for others.
When another user tries to modify the file, then press `:` and type `:wq!`, it promps and he says `y`, the ownership of the main file is changed too. Why??? The access from the main user is gone to another one.
![[Pasted image 20260304165047.png]]

